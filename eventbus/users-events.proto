syntax = "proto3";

package eventbus;

message Void {}

message User {
    string id = 1;
    optional string email = 2;
    optional string username = 3;
    optional string password = 4;
    optional string salt = 5;
}

message SearchUsersParams {
    optional string email = 1;
    optional string username = 2;
    repeated string usersIds = 3;
    optional int32 limit = 4;
    optional int32 offset = 5;
}

message SearchUsersEvent {
    SearchUsersParams searchParams = 1;
    repeated User users = 2;
}

service UsersEventsService {
    rpc getUserByIdEvent(User) returns (Void) {}
    rpc searchUsersEvent(SearchUsersEvent) returns (Void) {}
    rpc createUserEvent(User) returns (Void) {}
    rpc updateUserEvent(User) returns (Void) {}
    rpc deleteUserEvent(User) returns (Void) {}
}